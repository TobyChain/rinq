# Add to ~/.codex/config.toml (Codex CLI) to push turn completion to Rinq.
# The daemon must be installed (./install.sh) so the shim exists.
notify = ["/bin/sh", "-c", "exec \"$HOME/.local/bin/rinq\" hook-codex \"$1\"", "--"]
